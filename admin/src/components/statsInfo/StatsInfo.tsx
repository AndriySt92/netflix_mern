import React from 'react'

interface StatsInfoProps {
  month: string
  name: string
}

export const StatsInfo: React.FC<StatsInfoProps> = ({ month, name }) => {
  return <div className="statsInfo">
      </div>
}
